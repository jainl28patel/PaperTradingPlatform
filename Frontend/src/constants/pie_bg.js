const pie_bg = []
for (let i = 1; i >= 0.7; i -= 0.1) pie_bg.push(`rgb(45, 19, 119,${(i)})`)
for (let i = 1; i >= 0.8; i -= 0.1) pie_bg.push(`rgb(255, 220, 214,${(i)})`)
for (let i = 1; i >= 0.8; i -= 0.1) pie_bg.push(`rgb(234, 223, 248,${(i)})`)

export { pie_bg }